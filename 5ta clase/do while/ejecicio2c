#include <stdio.h>

int main(){ 

    int opcion;

    do {
        printf("Elige una opción: ");
        scanf("%d", &opcion);
        
        if (opcion == 1) {
            printf("Eligio la opción: 1\n");
        }
    } while (opcion == 1);
    
    return 0;
}
    